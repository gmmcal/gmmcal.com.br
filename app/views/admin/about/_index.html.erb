<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
  <h1 class="h2">About</h1>
  <div class="btn-toolbar mb-2 mb-md-0">
    <div class="btn-group mr-2">
      <%= link_to 'New', [:new, :admin, :about], class: 'btn btn-sm btn-outline-secondary' %>
    </div>
  </div>
</div>

<div>
  <p id="notice"><%= notice %></p>
</div>

<div class="table-responsive">
  <table class="table table-hover">
    <thead class="thead-dark">
      <tr>
        <th scope="col">#</th>
        <th scope="col">Job Title</th>
        <th scope="col">Translations?</th>
        <th scope="col"></th>
      </tr>
    </thead>

    <tbody>
      <% @abouts.each do |about| %>
        <tr>
          <th scope="row"><%= about.id %></th>
          <td><%= link_to about.job_title, [:edit, :admin, about] %></td>
          <td><%= t about.translations? %></td>
          <td>
            <%= link_to t(:show), [:admin, about], class: 'btn btn-sm btn-primary' %>
            <%= link_to t(:edit), [:edit, :admin, about], class: 'btn btn-sm btn-primary' %>
            <%= link_to t(:destroy), [:admin, about], class: 'btn btn-sm btn-danger', method: :delete, data: { confirm: 'Are you sure?' } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

