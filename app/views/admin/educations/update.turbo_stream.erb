<% if @education.changed? %>
  <%= turbo_stream.replace dom_id(@education), partial: 'form', locals: { education: @education } %>
<% else %>
  <%= turbo_stream.replace dom_id(@education), partial: 'education', locals: { education: @education.decorate } %>
<% end %>
