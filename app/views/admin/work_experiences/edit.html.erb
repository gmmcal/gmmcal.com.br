<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
  <h1 class="h2"><%= t(:edit, scope: %i[helpers]) %> <%= @work_experience.company_name %></h1>
  <div class="btn-toolbar mb-2 mb-md-0">
    <div class="btn-group mr-2">
      <%= link_to t(:back, scope: %i[helpers]), %i[admin work_experiences], class: 'btn btn-sm btn-outline-secondary' %>
    </div>
  </div>
</div>

<%= render 'form', work_experience: @work_experience %>
